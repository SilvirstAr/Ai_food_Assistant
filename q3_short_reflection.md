# Short Reflection - Q3 AI Food Assistant

งานนี้เริ่มจากการตีโจทย์ว่าไม่ได้ต้องการแค่รายชื่อร้านอาหาร แต่ต้องการ workflow ที่ใช้ข้อมูลจริงช่วยตัดสินใจเลือกร้านสำหรับทีม 8-12 คนใน 5 ย่าน ได้แก่ Ari, Siam, Thong Lo, Asok และ Phrom Phong ผมจึงเก็บข้อมูลด้วย Apify Google Maps Scraper ได้ทั้งหมด 200 ร้าน แล้วนำข้อมูลเข้า Google Sheets เพื่อแยก raw data, clean data, Top25 ต่อพื้นที่ และ Final Top3

ส่วนที่ทำได้ดีคือใช้ AI ช่วยจัดโครงข้อมูล, แยกย่าน, ให้คะแนนเบื้องต้น และสรุปเหตุผลของร้าน Top 3 ได้เร็วขึ้นมาก ข้อมูลไม่ได้มาจากการเดา เพราะมี Google Maps URL และ Wongnai URL เป็นหลักฐานตรวจย้อนกลับ โดยเฉพาะร้าน Top 3 ของแต่ละย่านที่มี source ที่ 2 จาก Wongnai

ข้อจำกัดหลักคือ Apify export รอบนี้ไม่มี price range ทำให้คะแนน Price Suitability ต้องใช้ค่ากลางและระบุ caution ว่าควรตรวจราคา เวลาเปิด และเงื่อนไขโต๊ะกลุ่มก่อนจองจริง อีกปัญหาคือบางชื่อร้านมีหลายภาษา ทำให้ต้องตรวจชื่อร้านและลิงก์หลักฐานซ้ำเพื่อไม่ให้ match ผิด

ส่วน automation ใช้ n8n อ่านข้อมูล Final_Top3 จาก Google Sheets ผ่าน CSV export URL แล้วสรุป Top restaurant ของแต่ละย่านอัตโนมัติ ผลลัพธ์ run สำเร็จ มี status OK, อ่านได้ 15 ร้าน และครอบคลุม 5 ย่าน จึงใช้เป็นหลักฐานว่า workflow ทำงานจริง ไม่ใช่ mockup

สิ่งที่ได้เรียนรู้คือ AI ช่วยทำงาน data workflow ได้มาก ตั้งแต่ clean data, scoring, analysis, HTML report และ reflection แต่ยังต้องมีมนุษย์ตรวจความถูกต้องของแหล่งข้อมูล ราคา ลิงก์ และบริบทการใช้งานจริงอยู่ดี งานชิ้นต่อไปควรวาง schema ตั้งแต่ตอน scrape ให้มี area/query/price ชัดเจนตั้งแต่ต้น เพื่อลดเวลาการ clean ภายหลัง
